# 不送進 build context 的東西

node_modules
**/node_modules
dist
.git
.vscode
.idea
*.log
*.tar
*.tar.gz

# v0.6.6: 環境變數檔不能進 Docker、不然 Vite build 時會把開發機 IP（譬如 Tailscale）
# 烘進 dist、讓內網 client 連不到。Docker build 用 Dockerfile 內 ENV 即可。
.env
.env.*
!.env.example

# 個資資料夾（病患資料夾等實體檔）
**/病患資料夾/
**/病患授權書/
**/下單Excel/

# dev-data/ 內含 seed JSON、builder 階段要編譯、所以不排除
# runtime stage 只 COPY dist + server、dev-data 不會進 final image
# 但 dev-data/* 內可能有個資、要意識到 builder layer 是 ephemeral
# 若擔心、build 完跑：docker image prune -f --filter "label=stage=builder"

# Build artifacts
build/
coverage/

# Backup files
*.bak
*.tmp
*.swp
*~

# OS
.DS_Store
Thumbs.db

# 開發機本地路徑（不該進 image）
D:/矯正
C:/矯正
