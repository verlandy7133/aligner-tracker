# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Real patient data — NEVER commit
dev-data/
**/patients-import*.json
**/patients-backup*.json

# v0.6.0 自動備份輸出（含 PII、絕對不能 commit）
backups/
aligner-tracker-backup-*.json

# v0.6.8 備份帳號憑證（BACKUP_USER / BACKUP_PASS、絕對不能 commit / 不進 Dropbox）
scripts/.backup-env

# Excel 自動匯入帳號憑證（SYNC_USER / SYNC_PASS、絕對不能 commit / 不進 Dropbox）
scripts/.sync-env

# Stage B build log (post-commit hook 寫入)
.stage-b-build.log

# Google OAuth 憑證（sync-google-sheet.mjs 用、絕不 commit）
.secrets/
